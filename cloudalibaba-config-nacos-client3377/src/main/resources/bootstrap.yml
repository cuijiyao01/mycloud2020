server:
  port: 3377

spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848   #服务注册中心和配置中心的地址
      config:
        server-addr: 127.0.0.1:8848   #服务注册中心和配置中心的地址
        file-extension: yaml

#dataId: ${prefix}-${spring.profiles.active}-${file-extension}
#prefix = spring.application.name


